var myVar = 'my variable';

function myFunction(x, y) {
    return x + y;
}

let num = 2;
const PI = 3.14;

var numbers = [1, 2, 3];
numbers.map(function (valor) {
    return valor * 2;
});

numbers.map(valor => {
    valor * 2
});

class Matematica {
    sum(x, y) {
        return x + y;
    }
}

var n1: any = 'sdsasdas';
n1 = 4;